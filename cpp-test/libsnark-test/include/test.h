#ifndef TEST_H
#define TEST_H

#include <string>

// Função que soma dois inteiros
int soma(int a, int b);

// Função que imprime uma mensagem
void imprime_mensagem(const std::string& mensagem);

#endif // TEST_H
